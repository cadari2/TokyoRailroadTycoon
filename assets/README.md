# Assets folder

Placeholder graphics are procedurally drawn until PNGs are dropped here.
See the `ASSET_MANIFEST` in `js/render.js` and the table in the root README
for required file names, sizes, and layer order. Files are picked up
automatically on load — no code changes needed.
